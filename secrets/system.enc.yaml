# SOPS-encrypted secrets for the pxe-host system.
#
# PLACEHOLDER. This file is intentionally an empty (unencrypted) YAML stub
# until real age public keys exist in `.sops.yaml`. Once those land, run:
#
#   sops --encrypt --in-place secrets/system.enc.yaml
#
# Add real key/value pairs above before the encrypt step. Keys to expect:
#   - root_password_hashed (used by templated answer-files)
#   - operator_ssh_pubkey
#
# DO NOT commit plaintext secrets here. The placeholder values below are
# explicitly fake.

placeholder: "replace-with-real-encrypted-content"
